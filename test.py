# Python program to demonstrate working
# of map.
  
# Return double of n
def addition(n):
    return n + n
    
# We double all numbers using map()
numbers = (1, 2, 3, 4)
result = map(addition, numbers)
print(list(result))




# Cantidad = Convert.ToInt32(textBox1.Text);

            # b1000 = Cantidad / 1000;
            # residuo = Cantidad % 1000;

            # b500 = residuo / 500;
            # b500Residuo = residuo % 500;

            # b200 = b500Residuo / 200;
            # b200Residuo = b500Residuo % 200;

            # b100 = b200Residuo / 100;
            # b100Residuo = b200Residuo % 100;

            # b50 = b100Residuo / 50;
            # b50Residuo = b100Residuo % 50;

            # textBox7.Text = b1000.ToString();
            # textBox8.Text = b50Residuo.ToString();
            # textBox2.Text = b500.ToString();
            # textBox3.Text = b200.ToString();
            # textBox4.Text = b100.ToString();
            # textBox5.Text = b50.ToString();
